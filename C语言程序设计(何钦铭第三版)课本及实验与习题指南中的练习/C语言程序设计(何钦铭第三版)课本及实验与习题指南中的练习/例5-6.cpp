//#include"Head.h"
//#include<cstdio>
//void add(float re1, float im1,float re2,float im2) {
//	char ch = '+';// 如果不满足if那么它还是 +
//	if (im1 + im2 < 0) ch = ' ';
//	printf("addition of complex is %f%c%fi\n", re1 + re2,ch, im1 + im2);
//}
//void prod(float re1, float im1, float re2, float im2) {
//	char ch = '+';
//	if (re1 * im2 + re2 * im1 < 0) ch = ' ';// ch = '-'这里如果本身结果是负的，那么就多了一个负号
//	printf("product of complex is %f%c%fi\n", re1 * re2 - im1 * im2, ch, re1 * im2 + re2 * im1);
//}
//int main() {
//	float re1 = 0, re2 = 0, im1 = 0, im2 = 0;
//	printf("Enter 1st complex number (real and imaginary): ");
//	scanf("%f%f", &re1, &im1);
//	printf("Enter 2nd complex number (real and imaginary): ");
//	scanf("%f%f", &re2, &im2);
//	add(re1, im1, re2, im2);
//	prod(re1, im1, re2, im2);
//	return 0;
//}
//
